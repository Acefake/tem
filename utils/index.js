import { isEmpty } from './is_empty';
import { getDict } from './dict';
import { envInfo } from './env'
import toast from './toast';

export default {
    isEmpty,
    envInfo,
    getDict,
    toast,
}